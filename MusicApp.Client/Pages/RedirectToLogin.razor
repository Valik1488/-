@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Hard-code the navigation without any returnUrl to break the loop
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}